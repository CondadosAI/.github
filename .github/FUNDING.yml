# Org-wide funding links: GitHub serves this file as the default for every
# CondadosAI repository that does not ship its own, which is what puts the
# "Sponsor this project" card in the sidebar.
#
# The card only renders once the account below has a GitHub Sponsors listing
# enabled; until then this file is inert rather than wrong.
github: [Gabriellgpc]
